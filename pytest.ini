[pytest]
python_files = test_*.py
norecursedirs = tools scripts .venv
# Exclude long network-specific tests by default; keep them available for local runs
# addopts = -k "not e2e and not paystack and not email"
